import { DOMAIN_CONTRACTS_VERSION } from '@fcm/domain-contracts';
import { SCORING_CORE_VERSION } from '@fcm/scoring-core';

export const API_CONTRACTS_VERSION: string = DOMAIN_CONTRACTS_VERSION;
export const API_SCORING_VERSION: string = SCORING_CORE_VERSION;
